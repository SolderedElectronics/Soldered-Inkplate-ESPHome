esphome:
  name: inkplate13-test
  friendly_name: Inkplate 13 Test

esp32:
  board: esp32dev
  framework:
    type: esp-idf

external_components:
  - source:
      type: local
      path: components

logger:

spi:
  clk_pin: GPIO18
  mosi_pin: GPIO23
  miso_pin: GPIO19

inkplate13:
  id: my_inkplate13
  cs_pin: GPIO15
